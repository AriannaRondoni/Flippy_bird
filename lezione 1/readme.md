Setup e Inizializzazione
l'obiettivo è di preparare l'ambiente di gioco e definire le costanti di base.
in questo codice, vengono importate le librerie necessarie (pygame, sys, random), inizializza Pygame, definisce le dimensioni della finestra (LARGHEZZA, ALTEZZA),
crea la finestra e definisce la palette di colori RGB che verranno utilizzati nel gioco (es. AZZURRO per lo sfondo, ROSSO per l'uccello).
 
 
